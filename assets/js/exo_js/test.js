//exercice8 (exo3)
var nbre = parseInt(prompt("saisir un nombre"));
var resultat=nbre+nbre;

while (nbre > 0) 
{   
    // Exécute le calcul et stocke le résultat   
    // dans une variable nommée ‘resultat’  
    resultat = nbre;

    // A chaque tour, on affiche le résultat courant à l’utilisateur
    console.log("resultat");

    // A chaque tour, on ajoute +1 à la variable i  
    nbre++; 
    continue;

    if (prompt==0)

    {
        break;
    } 
}

var total=parseInt("nbre"+"nbre");

console.log(total);
console.log(moyenne);
console.log(resultat);
alert=moyenne;
alert=resultat;



















